clc;clear all;close all;# Affichage image et rotation 90° A = imread("kawaii_ice.jpg"); % On enregistre l'image sous forme matricielle# imshow(A) % Afficher l'image for i=1:rows(A)  for j=1:columns(A)        newI = j; # Nouvelle ligne i = Colonne j (de la matrice de base)         newJ = rows(A) - i + 1; # Nouvelle colonne j = nombre de lignes de la matrice de base (nombre de colonnes de la matrice résultante) - i + 1 (index 0)        newA(newI, newJ, :) = A(i, j, :); # 3 dimensions : RGB  endforendfor imshow(newA)